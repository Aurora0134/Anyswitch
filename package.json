{
  "name": "anyswitch",
  "version": "0.5.2-preview",
  "description": "Local AI credential relay for Windows: funnels OpenAI-compatible upstreams into a loopback relay with DPAPI-sealed API keys",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.15.0 <23 || >=23.8.0"
  },
  "os": [
    "win32"
  ],
  "keywords": [
    "relay",
    "api-credentials",
    "llm",
    "openai-compatible",
    "anthropic",
    "dpapi",
    "windows",
    "localhost"
  ],
  "scripts": {
    "test": "node --test *.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aurora0134/Anyswitch.git"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.1.0"
  }
}
